name: Ephyphonic Heartbeat
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Orquestrator Cron Waker
        run: 'curl -X GET "https://ephyphonic-hybu.vercel.app/api/worker" -H "Accept: application/json"'